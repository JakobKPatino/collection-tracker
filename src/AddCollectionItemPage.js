function AddCollectionItemPage({handleWindowBack}) {
  return (
    <div>
      <p>Add Collection Item</p>
      <button onClick={handleWindowBack}>Back</button>
    </div>
  )
}

export default AddCollectionItemPage;